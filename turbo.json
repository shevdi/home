{
  "$schema": "https://turbo.build/schema.json",
  "globalPassThroughEnv": ["BACKEND_URL"],
  "globalEnv": ["BACKEND_URL"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    }
  }
}
